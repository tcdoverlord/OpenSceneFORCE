# State Machine

See previous draft: runtime states, transitions, validation gates,
SAFE_MODE behavior, and state storage in `app/state/state.py`.

This placeholder is included so all three files are downloadable
together.
